import React from 'react'
import UserCard from './UserCard'

export default function UserList({ data }) {
    return (
        <div>
            {/* {data && data.map(item => {
                return (
                    <UserCard key={item} data={item} />
                )
            }
            )} */}
        </div>
    )
}
